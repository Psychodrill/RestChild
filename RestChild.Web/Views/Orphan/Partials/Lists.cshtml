@model RestChild.Web.Models.Orphans.OrphanageModel

<div role="tabpanel" class="tab-pane @(Model.ActiveTab == "Lists" ? "active" : string.Empty)" id="Lists">
    @using (Ajax.BeginForm("OrphanagePupilGroupListsSearch", null, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "ListsForm", InsertionMode = InsertionMode.Replace, OnSuccess = "InitSelects" }, new { id = "ListsForm" }))
    {
        @Html.Partial("Partials/ListsList", Model.Lists)
    }
</div>

